type Product {
    id: ID
    productDisplayName: String
    masterCategory: String
    subCategory: String
    baseColour: String
    similarity: Float
}

type Query {
    searchProducts(query: String!, topN: Int!): [Product]
}
